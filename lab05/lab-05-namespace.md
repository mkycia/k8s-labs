# LAB-05

```
1. kubectl create ns lab-nginx

2. kubectl run lab-nginx --image=nginx -n lab-nginx

3. kubectl get pods -n lab-nginx

4. kubectl describe pod lab-nginx -n lab-nginx

5. kubectl config get-contexts

6. kubectl config set-context --current --namespace=lab-nginx

7. kubectl config get-contexts

8. kubectl get pods 

9. kubectl describe pod lab-nginx

10. kubectl config set-context --current --namespace=default

11. kubectl config get-contexts
```